# Handoff

This document exists so any AI agent (or human) picking up this project can understand the context, the vision, and how we work together — without re-deriving it from scratch by reading the whole chat history. Read this first. Then read `CHANGES.md` for the detailed, chronological log of what's actually been built and why (including bugs found and how they were diagnosed).

## What this is

A **private fork of Ultrasonic** (the open-source Android Subsonic/Navidrome/Airsonic client), cloned for the user's personal use. Not intended to be pushed to the public GitLab/GitHub upstream or shared. `origin` still points at `gitlab.com/ultrasonic/ultrasonic` — never push there.

## The vision

Turn Ultrasonic from a generic, feature-complete Subsonic client into a **music-only app built for intentional listening**, visually inspired by Spotify. Concretely, that means:

- **Music only.** Podcasts, Video, and Chat have been hidden from the UI (drawer + Home quick-access) because they don't belong in a "pure music player." Their code was deliberately *not* deleted — video support in particular is threaded through shared classes (`Track`, downloads) that music also depends on, so ripping it out is a much bigger, riskier job than hiding a menu entry. If asked to go further, that's a distinct, larger task — don't assume it's wanted.
- **Local player, not a social music network.** The user's own words: "que se sienta más como reproductor local... sin necesidad de estar conectado... no una red social de música" — like putting on a CD and reading the cover, not browsing a feed. Connectivity is for *fetching your own library and its metadata*, not for sharing, discovery feeds, or anything socially-facing. This is why **Share was removed app-wide** (song/playlist/queue share links, the whole "Shares" drawer screen) — hidden from UI, code kept, same pattern as Podcasts/Video/Chat. Weigh any future feature against this: does it help the user play and enjoy music they already have, or does it add a social/networked layer they didn't ask for?
- **Calm over algorithmic.** No fake personalization. Every "smart" feature (Mix, Discover, Recently Played) is built from data the Subsonic API already exposes (genre lists, play history, starred items) — never a recommendation engine. When in doubt, prefer showing the user their own listening history over inventing content.
- **Minimalist over feature-dense — when in doubt, check real Spotify.** The user has pushed back twice now on screens/rows that accumulated too many simultaneous icons/buttons (the album track row went from 3 per-row icons down to a single "⋮" after comparing side-by-side with Spotify screenshots). Default to *fewer* visible controls, with secondary actions behind a single overflow menu, not spread across the row. If unsure whether something is "enough" vs "too much," a real Spotify screenshot of the closest equivalent screen is the fastest way to check before building.
- **Established visual language** (built up incrementally, screen by screen — see "Design system" below).
- **Open vision, not yet detailed: replace the Player's top toolbar and the left navigation drawer with icon-based navigation.** The user doesn't like the current left drawer dropdown. Mentioned in passing while discussing the Player's queue-toggle button placement (2026-08-08) — no concrete design yet, just a direction. See "Current state" item under Player (queue button/secondary row) for the one concrete consequence already acted on: a toolbar action was deliberately left duplicated with a new button because it'll become redundant once this happens, not removed now. Ask for specifics before starting real work on this.
- **Out-of-the-box, zero-config app — the only thing a user should ever configure is the server.** The user's stated end goal (2026-08-08), sharpened significantly the same day when they framed *who this app is actually for*: not the person who sets up the Subsonic server (a "geek"), but whoever that person shares the server with to just listen — a parent, a young cousin, anyone who doesn't know or care what FLAC vs. MP3 or bitrate numbers mean. Their own words: "creo que los clientes de servidores fallan por eso, porque están pensados en gente geek que desarrolla el servidor no en los usuarios comunes." This has driven the removal of technical/dead settings, the fixed Taki palette, and the conversion of raw bitrate lists into five friendly quality tiers while retaining compatible stored values.

## How we work together

- **Antes de compartir una APK o crear un release, seguir `docs/POSIBLES_ERRORES_Y_VERIFICACION.md`.** Incluye las pruebas obligatorias de reproducción, restauración con colas grandes, threading de Media3, navegación y logcat. Esta verificación es especialmente importante después de tocar `MediaPlayerManager`, `PlaybackStateSerializer`, `MediaPlayerLifecycleSupport`, `PlaybackService`, `PlayerFragment` o `NowPlayingFragment`.

- **Build and device verification are available.** Use Android Studio's bundled JBR (`C:\Program Files\Android\Android Studio\jbr`) with `./gradlew.bat :ultrasonic:assembleDebug`. A Pixel 7 has been available through ADB for installation, UI hierarchy inspection, screenshots, and navigation checks; verify that it is still connected before relying on it. Continue to distinguish clearly between build verification, device interaction, and behavior that still needs user testing.
- **The project lives inside a OneDrive-synced folder, and that can break Gradle mid-session.** OneDrive's "Files On-Demand" can turn a just-written build output file into a cloud placeholder (an NTFS reparse point) instead of a regular file — Gradle's file snapshotting then fails with `Cannot snapshot <path>: not a regular file`, on whatever generated file it touches next (data-binding classes, KSP output, `.class` files all hit this in one session, each a different file on each retry — it's not scoped to one task). **Fix: delete the affected `build/` directories (`ultrasonic/build`, `core/*/build`, root `build/`) and rebuild** — regenerated files come back as normal local files, at least for a while. `--no-build-cache`/`--no-configuration-cache` alone are not reliable fixes on their own (tried first, still failed) — only a clean `build/` wipe resolved it. If this recurs often, the real fix is excluding `build/` from OneDrive sync (or moving the repo out of OneDrive entirely) — worth raising with the user if it keeps happening.
- **Iterative and screenshot-driven.** The user shares a screenshot, gives specific feedback ("this shadow looks like a floating circle", "the corners are too round"), and expects a concrete fix, not a discussion — unless the request is genuinely ambiguous or has a real design tradeoff, in which case ask first (see next point).
- **Ask before guessing on ambiguous or costly decisions.** Examples from this project: how to pick which artist/genre for the Mix, whether the Mix should regenerate every refresh or persist, how rounded corners should be, whether to hide or delete non-music features. Use targeted questions (2-4 options, not open-ended) rather than picking silently. For unambiguous, well-scoped asks, just implement — don't over-ask.
- **Reuse existing patterns aggressively.** This codebase has established idioms for almost everything: `RefreshableFragment` + `toastingExceptionHandler()` for loading/error state, `BaseAdapter`/MultiType `ItemViewDelegate` classes for RecyclerView rows, `SettingsDelegate` subclasses (`StringSetting`, `BooleanSetting`, etc.) for SharedPreferences-backed state, `NavigationGraphDirections`/`PlayerFragmentDirections` for Safe Args navigation. **Search for the existing pattern before inventing a new one** — several bugs in this project came from not doing that carefully enough (see the bug write-ups in `CHANGES.md`, e.g. the `navArgs()` crash from bypassing a Safe Args action).
- **No git commits or pushes without being explicitly asked.** For most of this project the user didn't want commits at all ("private app, probably won't commit"). Only commit when asked, as a single commit unless told otherwise, and never push anywhere without separate explicit instruction.
- **Keep `CHANGES.md` updated.** Every change gets logged there as it happens, grouped by topic, in Spanish (matching the conversation language), with file paths. Treat it as the project's real changelog — update it in the same turn you make the change, not as an afterthought.
- **Don't over-scope.** When a request implies a much bigger job than the rest of the conversation's pace (e.g. "apply the Home style to every screen" after just finishing Home itself), say so plainly and propose tackling it incrementally rather than silently doing a huge sweep.

## Design system (established incrementally — treat as the current baseline)

- **Surfaces:** neutral dark tones from the Material3 role system (`colorSurfaceContainerHigh` / `colorSurfaceContainerHighest`), not saturated/tonal colors like `colorSecondaryContainer`.
- **Corners:** deliberately more squared than stock Material3 — **4dp** on cards (carousel cards, shortcut grid cards), 16-20dp only on large floating panels (mini player, player control panel, drawer edge). This was an explicit user preference ("mi estilo, apenas suavizado el borde"), not a Material3 default.
- **Text hierarchy:** primary text (song/album/mix title) is **bold**; secondary text (artist, subtitles) is regular weight and muted (`colorOnSurfaceVariant`). Applied to Home's cards. The mini player's title is deliberately *not* bold — it was tuned down from bold earlier for being "too bold" relative to its small cover art, so don't naively re-apply the bold-title rule there without checking with the user.
- **Depth without native elevation on transparent icon buttons.** Android `elevation` draws a shadow from the view's *outline*, not its visible pixels — on a transparent-background `Widget.Material3.Button.IconButton`, that produces a floating disconnected circle (a real bug found and fixed here). Where a subtle shadow is wanted on an icon, it's hand-drawn: the icon is rendered twice in a `<layer-list>`, a dark low-alpha copy offset 1dp behind the normal one (see `media_*_shadow.xml`). Where a shadow is wanted on a whole card/panel (mini player, player control panel), use real `elevation` on a view that actually has an opaque/rounded background — that works fine.
- **Floating card pattern** (mini player): inset from screen edges via `layout_margin`, rounded corners via a dedicated background drawable (not a `MaterialCardView`, just a `<shape>`), slight transparency via `android:alpha` on the whole view (not a true blur — that's a bigger, unimplemented ask if ever wanted).
- **Minimal metadata on the Player screen.** Only title, artist, and playback position/duration are shown. Everything else (album, genre, bitrate, track-in-queue counter, total queue duration) was deliberately removed as clutter; tapping the title navigates to the album where full details live. If asked to add more info back, prefer a tap-through/secondary view over cramming it into the main screen.
- **`FloatingActionButton` is not circular by default under Material3** (it defaults to a fixed rounded-square corner, unlike Material2's full circle) — a real bug found on Artist Detail's and Album Detail's play buttons, which looked like an "out of place" rounded square instead of the intended circular play button. Fix: `app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.Ultrasonic.Circle"` (already defined in `styles.xml`, previously only used for circular avatar images). Apply this to any new circular FAB.
- **Icon sizing: use `MaterialButton` (`style="@style/Widget.Material3.Button.IconButton"` + `app:icon`/`app:iconSize`/`app:iconTint`), never a plain `ImageButton` with manual `android:padding` to fake a size.** The padding trick was used for the "⋮" and per-disc download icons and consistently rendered blurry/oversized regardless of the padding math — `ImageButton`'s default `scaleType` doesn't reliably constrain a vector drawable's intrinsic size the way `app:iconSize` does. Found and fixed across `list_item_track.xml`, `list_item_track_album.xml`, and `disc_header_item.xml`; watch for the same pattern anywhere a new small icon-only button gets added.
- **When a screen's top-level list isn't `Track`, extend `MultiListFragment<T>` directly instead of `TrackCollectionFragment`.** `TrackCollectionFragment` carries a lot of Track-specific machinery (selection mode, "⋮" context menu, per-track binders) that buys nothing for a screen listing a different item type, and registering a second delegate for a type `TrackCollectionFragment` already registers unconditionally (e.g. `Album` via `AlbumRowDelegate`) risks an ambiguous override instead of a clean replacement. `MultiListFragment<T>` (the shared base both classes sit on) already provides swipe-refresh, empty state, and the RecyclerView/adapter wiring generically — see `DownloadsFragment` (album cards) vs. `ArtistDetailFragment`'s own separate `albumsAdapter` for its discography grid, both examples of the same pattern.
- **The app's real "offline mode" (`ActiveServerProvider.isOffline()`, a dedicated "Offline" server entry) is a separate concept from "screens that read local/downloaded data."** Offline mode is a global switch that redirects the *entire* app (via `MusicServiceFactory`) to `OfflineMusicService`, hiding network-only actions everywhere (rating, download, add-to-playlist, lyrics, etc.). A screen can read from `offlineMetaDatabase` directly and work with no network regardless of that switch — Downloads does this deliberately, since its whole point is to work even when the user is mid-session online but the thing they tapped happens to only be partially cached. Don't conflate "this screen is local-only" with "the user is in offline mode" when writing future screens.
- **User decision (2026-08-09): keep Offline explicit.** An automatic fallback was investigated and implemented experimentally, then reverted at the user's request because it made the active collection and navigation model difficult to understand. `Offline` remains the explicit first entry in the configured-server selector and may internally retain its legacy `http://localhost` identity. Do not automatically switch `Settings.activeServer` when connectivity is lost or the real server stops responding. While a real server remains selected, `CachedDataSource` already checks the complete downloaded file before opening the network stream, so downloaded tracks in the current screen/queue can continue to play without selecting Offline; selecting Offline is only required to browse the full local catalogue through `OfflineMusicService`. If a track has no complete local copy, playback still requires the server. Revisit automatic fallback only as a separately designed feature with clear UI and queue/database transition rules.
- **Never implement `CoroutineScope by CoroutineScope(Dispatchers.Main)` (or any fixed-instance delegate) on a `Fragment` whose view gets destroyed/recreated while the Fragment instance itself survives (any Nav Component backstack pop-back scenario).** `PlayerFragment` did exactly this and cancelled that single delegate in `onDestroyView()` — since the delegate expression only evaluates once, at Fragment construction, every `launch { ... }` call in the file silently became a permanent no-op the moment the user navigated away and back once (Player → Lyrics → Player, but really *any* screen), with zero exceptions or log output to point at. Found via a real user report ("los botones y la barra de avance no responden" after visiting Lyrics) — a real, reproducible pre-existing bug, not something the Lyrics work introduced; it just became far more likely to trigger once Lyrics got an easy, frequently-used access point. Fixed by implementing `CoroutineScope` manually (not via `by`) backed by a reassignable `private var mainScope`, recreated at the top of every `onCreateView()` — this required zero changes to any of the file's many existing `launch { ... }` call sites, since `coroutineContext` resolves against the live `mainScope` dynamically on each access. **Grep for `CoroutineScope by CoroutineScope(` if this pattern turns up in another Fragment** — it's a trap that works perfectly the first time a screen is visited and silently breaks forever after the first round trip.
- **Open, deliberately unresolved: `Ultrasonic.PrimaryText` (`sans-serif-medium`) reads as less crisp/defined than `Ultrasonic.SecondaryText` (`sans-serif-light`) at small sizes on this app's dark surfaces.** Flagged by the user after the Album Detail text-size pass, left as-is for now (explicitly: "dejemos así por ahora pero documenta"). Likely cause: light-on-dark text has a well-known optical "irradiation" effect where stroke edges appear to bleed/halo into the dark background, and it's more pronounced the thicker the stroke — so a medium weight looks comparatively muddier than a light weight at the same small size, even though on a light background the effect would likely read the opposite way. If revisited, the option discussed was dropping title weight further (e.g. to plain `sans-serif` or even reusing `sans-serif-light`) — but `Ultrasonic.PrimaryText` is used app-wide (Home, Artist Detail, Album Detail, mini player, etc.), so treat that as a global design-system change, not a one-screen tweak, and confirm with the user first.

## Current state (high level — `CHANGES.md` has the full detail, including every bug found)

1. **New Home screen**, now the app's start destination (`homeFragment` in the nav graph). Greeting, quick-access buttons, "Recently Played" shortcuts grid, a **daily-persisted genre Mix** (one playlist-style row, regenerates once per calendar day via `Settings.homeMix*` SharedPreferences keys — not a carousel, and not regenerated on every refresh), and album carousels: Favorites, Recently Added, Discover (renamed from "Random" — but that string rename is Home-only, the shared `main.albums_random` string used elsewhere, e.g. sort-order dropdowns, was deliberately left alone).
2. **Drawer & toolbar** modernized.
3. **Mini player** redesigned as a floating card with prev/play/next (was play/pause only).
4. **Full Player screen** trimmed to title/artist/progress; 5-star rating removed in favor of a single-line heart.
5. **Podcasts/Video/Chat hidden** from UI only.
6. Debug builds are labeled "ultrasonic-test".
7. **Media Library redesigned**: Home now exposes horizontal shortcuts for Playlists, Artists, Albums, Songs, and Genres; artist and album lists support compact list/grid views and shared filtering; Songs has library-specific filters and playback behavior; Genres uses artwork-backed cards and opens its songs correctly.
8. **Playlists are functional and redesigned**: server playlists open normally, new playlists can be created by selecting songs, download/removal state is visible, and list/grid modes use four-cover collages. List mode uses genre-inspired horizontal cards; grid mode uses two-column square artwork with labels below.
9. **Artist detail is now dedicated and functional**: selecting a real artist opens `ArtistDetailFragment` instead of the generic album grid. It has a full-width artwork/gradient hero, scroll-revealed toolbar title, play/download-all actions, five compact song shortcuts, and a horizontal album shelf. `getTopSongs` supplies the real Popular ranking when available; `getArtistInfo2` optionally adds an expandable biography and similar-artist carousel. Both endpoints fail softly to the existing search/album data when the server or its external metadata agents cannot supply them. Index/folder entries still follow the legacy track-collection route.
10. **Album detail** now has its own Spotify-style hero (art + gradient + scroll-revealed title), a play/shuffle/download row, and restyled track rows (always-visible track numbers, disc prefix for multi-disc albums, artist hidden for single-artist albums, no per-row rating/cover) — scoped to `TrackCollectionFragment` with `navArgs.isAlbum == true` only, via `AlbumDetailHeaderBinder`/`album_detail_header_item.xml`/`list_item_track_album.xml`. Playlists, genre, artist-songs, starred, all-songs and video keep the original generic header/row untouched (`HeaderViewBinder`/`list_header_album.xml`, `list_item_track.xml`). It also has an optional expandable "About" section (album notes/review) via `getAlbumInfo2`, mirroring Artist Detail's biography — same soft-fail behavior, hidden when unsupported/empty.
11. **Bug audit fixed**: a Home shelf failing no longer blanks the whole screen (each fetch is now caught independently instead of sharing a `coroutineScope` that cancels siblings); Artist Detail's track-fallback chain no longer aborts the whole load when it fails (its documented "soft-fail" behavior now actually holds); genre-song pagination no longer risks skipped/duplicated tracks on fast scrolling; rapid filter/sort switching in Library and Create Playlist no longer risks a stale response overwriting a newer one (both now cancel the previous load job before starting a new one, mirroring `ArtistListModel.setSortOrder()`'s existing pattern).
12. **Track-row interaction model overhauled app-wide** (Album Detail, Playlists, Genre, artist songs, Starred, All songs, Bookmarks — everywhere except the Songs media-library tab, which already had its own row): tapping a track now plays it immediately (previously it toggled a selection checkbox — tap never actually played a song directly in these screens, a real pre-existing bug). Long-press enters a Gmail/Photos-style selection mode instead of opening the context menu, with a circular Spotify-style check indicator (`btn_check_circle.xml`) instead of the old square checkbox, and pressing back while in selection mode exits the mode (clears selection) instead of navigating away — an early version of this got that wrong and was reported/fixed. Outside selection mode, each row shows a single "⋮" icon (opens the same context menu that used to open on long-press — no actions were dropped, "Add to playlist" was added to that menu). An earlier pass had 3 per-row icons (download/add-to-playlist/⋮); that was simplified back down to just "⋮" after comparing against real Spotify screenshots — see the "Minimalist over feature-dense" vision point above. The bottom selection bar shrank from 7 buttons to 2 (download, add to playlist); bulk pin/unpin/delete/play-next/play-last are no longer available for a multi-selection — only per-track via the "⋮" menu. Flag this trade-off to the user if it comes up again; restoring bulk versions of those actions would be a separate follow-up.
13. **"Add to an existing playlist" now exists** (`util/PlaylistUtil.kt`) — it didn't before (only "create a new playlist" existed). Subsonic has no append endpoint, so it works by re-fetching the target playlist's current songs and resubmitting the full list via `createPlaylist(id = ...)`, which Subsonic treats as an overwrite when an id is given.
14. **Share removed app-wide** (song/playlist/queue share links + the "Shares" drawer screen) — hidden, code kept (`ShareHandler`, `SharesFragment` still exist, just unreachable). See the "Local player, not a social network" vision point above for why.
15. **Album Detail: second on-device feedback round.** Track numbers are no longer force-shown on album rows — they now respect `Settings.shouldShowTrackNumber` like every other screen (default off), reverting the earlier "always show, with disc-prefix like '1-03'" decision. For multi-disc albums, disc grouping is now a small divider row ("Disc N" + a line, new `DiscHeader` item type/`DiscHeaderBinder`) inserted between tracks at disc boundaries, instead of a number prefix. The header's standalone download button was removed (only shuffle + play remain there); whole-album download is via multi-select + the bottom bar, or per-track via "⋮". Added breathing room between the selection checkbox and the track-number column.
16. **Downloads is a real download manager**, not just a functional track list. It went through two rewrites:
    - First: made functional at all — it was read-only before (no tap, no context menu, no delete/unpin; the original devs had left TODOs about this), rebuilt on `TrackCollectionFragment` like `BookmarksFragment`. Its data source was also corrected after an on-device bug report (tracks downloaded in a previous session showed nothing): the first version used `DownloadService.observableDownloads`, which only reflects the *active download queue* — a track drops out of it the moment its download finishes, it's a progress indicator, not a downloaded-library catalog. **If any screen/feature needs "what's actually downloaded," always read `activeServerProvider.offlineMetaDatabase.trackDao()`, never `observableDownloads`.**
    - Second, after the user clarified they wanted it to look like Playlists (cards, not a flat list) and confirmed it should function as a proper download manager: **`DownloadsFragment` now extends `MultiListFragment<Album>` directly, not `TrackCollectionFragment`.** It shows one card per downloaded album (`DownloadedAlbumRowBinder`/`list_item_downloaded_album.xml`, same visual language as Playlists' cards), grouped/counted from the same `trackDao()`/`albumDao()` local database, each with its own remove-download action. Tapping a card opens `DownloadedAlbumFragment` (new, thin `TrackCollectionFragment` subclass, `isAlbum = true`) which reuses the existing Album Detail hero/disc-grouping UI but loads tracks via `getDownloadedAlbumTracks()` — **always local-only, deliberately not the normal `toTrackCollection`/`getAlbumAsDir` network path**, so the whole flow keeps working with zero connectivity instead of erroring out or showing tracks that were never actually downloaded for a partially-grabbed album. `MultiListFragment` (not `TrackCollectionFragment`) turned out to be the right base whenever a screen's *top-level* list isn't tracks — see the new design-system note below.

17. **Theme picker removed — the app is dark-only now, no user setting.** `Settings.theme` and its 4 variants (`UltrasonicTheme.DayNight/.Dark/.Black/.Light`) are gone; `Util.applyTheme()` always applies `UltrasonicTheme.Dark`. Also removed as dead weight once nothing could trigger it: the `RxBus.themeChangedEvent*` publisher/observable and the `NavigationActivity` subscription that used to call `recreate()` on theme change. Still uses `Theme.Material3.DynamicColors.Dark` as the parent (so `colorPrimary` etc. still follow the device wallpaper) — see the vision note above for the follow-up (a real fixed palette) this sets up but doesn't do yet.

18. **Settings: "Appearance" category removed entirely, hardcoded instead.** Direct extension of the out-of-the-box vision (see above) — driven by real device feedback on the Settings screenshot. `Settings.kt`'s `shouldDisplayBitrateWithArtist`, `shouldUseFolderForArtistName`, `shouldShowTrackNumber`, `serverScaling` are now `const val`s fixed at their prior real behavior (not necessarily the `BooleanSetting` delegate's fallback default — a few of those didn't actually match the XML-declared default or what the screenshot showed, so the *actual current behavior* was used as source of truth, not either declared default). `shouldSortByDisc` is the one real behavior change: fixed to `true` (was `false`), confirmed explicitly — sorting by disc is strictly more correct for multi-disc albums and has no effect on single-disc ones. `showNowPlayingDetails` was deleted outright rather than fixed — already dead code, nothing reads it since those fields left the Player screen earlier this session. **Before hardcoding anything under "Appearance," each option was checked for whether it was actually a style preference vs. a server/library-compatibility switch in disguise** — `shouldUseFolderForArtistName` depends on whether the user's server library has proper ID3 tags, confirmed with the user before fixing it, not assumed. Also removed as a side effect: the entire "Appearance" `PreferenceCategory` (now empty) and the dead "Show details in Now Playing" switch under "Playback Control."
19. **Search: live filtering as you type, debounced.** Search hits the server on every query (no local list to filter client-side), so true per-keystroke requests would hammer it — `NavigationActivity.scheduleLiveSearch()` waits 400ms of typing pause, requires 2+ characters, and skips repeated/unchanged queries before navigating to `searchFragment` (using `NavOptions` singleTop + popUpTo so refining a query replaces the existing back-stack entry instead of stacking one per keystroke). Explicit submit (IME search key, voice search) is untouched — still the original `ACTION_SEARCH` intent path, which is also the only path that saves to recent-search suggestions (live keystrokes deliberately don't). `SearchFragment.search()` also now cancels its previous in-flight `Job` before launching a new one, so a slow response to an old query can't land after and overwrite newer results. Only launch-tested (no crash), not interactively verified on a device — the debounce feel/timing in particular needs a real trial.
20. **Settings: second, much larger simplification pass — ~15 more settings removed.** Triggered by the user's reframing of who the app is for (see vision section). Every remaining category was audited setting-by-setting for "would a non-technical listener ever touch this" before removing anything — an `Explore` agent traced every single call site first so nothing broke silently. Fixed-and-removed: `resumePlayOnHeadphonePlug=true`; Bluetooth resume/pause collapsed from a 3-way user choice (all-devices/A2DP-only/disabled) down to fixed A2DP-only for both (`BluetoothIntentReceiver.kt` simplified accordingly, the custom picker dialog and `Constants.PREFERENCE_VALUE_*` deleted from `SettingsFragment.kt`); `showNowPlaying=true` (the whole now-empty "Notifications" category went with it); `chatRefreshInterval` fixed at its old default purely so the already-unreachable `ChatFragment.kt` keeps compiling; `preloadCount=3`, `parallelDownloads=3`, `directoryCacheTime=300`; all 6 Search count settings fixed at their old defaults (`defaultArtists=3/maxArtists=10/defaultAlbums=5/maxAlbums=20/defaultSongs=10/maxSongs=25`) — **note `maxAlbums`/`maxSongs` double as generic page-size defaults well beyond Search** (Browse grids, "Play Random Songs," playlist creation, any track-collection screen without an explicit size), so their old values were kept exactly to avoid changing behavior anywhere else; `seekInterval=5000`ms (the `seekIntervalMillis` computed property is actually *seconds* despite its name — pre-existing naming bug, documented not fixed, both units are genuinely used by different playback paths); `shouldShowArtistPicture=true`; `scrobbleEnabled=true` (no per-server capability flag exists for scrobble, unlike chat/bookmarks/shares, so there's nothing to auto-detect — it's now unconditional and a no-op on servers with nothing configured to scrobble to). **Deleted outright** (not just hardcoded): the "Network Timeout" setting — turned out nothing in the entire codebase reads it, a fully dead preference row; "Hide media from other apps" — had no `Settings.kt` property at all, lived only as a reactive `.nomedia`-folder toggle in `SettingsFragment.kt` with no startup-time enforcement, removed per explicit user preference (they don't want their music hidden from other apps). Categories left standing (Playback Control, Network, Music Cache, Other, Debug) kept everything that's a genuine functional choice (ID3 tag mode, bitrate caps, ReplayGain, hardware offload, language override) — those are compatibility/preference decisions, not tuning noise. Only launch-tested, the Settings screen itself has not been reopened and eyeballed since this pass.

21. **Settings regrouped to match a reference app's structure, Equalizer surfaced.** `settings.xml`'s categories are now Now Playing / Library / Images / Network / Music Cache / Other / Debug (was Playback Control / Network / Music Cache / Search / Other / Debug) — purely a reshuffle, nothing removed. The Equalizer (`EqualizerFragment`/`EqualizerController`, fully built, previously only reachable from the Player's overflow menu) now has a direct entry in "Now Playing" via a new global `toEqualizer` nav action. Four other reference-app features were investigated but **deliberately not built** per explicit user pullback ("simplifiquemos un poco, no nos metamos tan adentro por ahora" — 2026-08-08) after the investigation turned up more complexity than the original comparison doc assumed:
    - **Volume slider**: would collide with ReplayGain, which already overwrites `player.volume` on every track change (see `PlaybackService.updateReplayGain()`) — needs to multiply with that, not replace it.
    - **"Cache images" toggle**: there are two independent caching layers (Coil for on-screen thumbnails via `ImageLoader.kt`, plus a separate hand-rolled disk cache for full covers in the same file) — a single on/off needs a decision about which layer(s) it controls.
    - **Duck volume instead of pausing on audio focus loss**: `PlaybackService.kt` already sets `.setAudioAttributes(attrs, true)` (`handleAudioFocus = true`), and Media3's built-in `AudioFocusManager` may already duck automatically for transient "can duck" focus loss — this needs on-device verification before writing any new code, it might already be the current behavior.
    - **Export playlist to file**: the app already reads/writes `.m3u` files, but only with local file paths for downloaded tracks (offline mode) — a "real" export of a server playlist needs a decision on what to write instead (streaming URLs would embed the user's auth token, a privacy concern).
    All four remain a real comparative-analysis artifact from this session (published, not saved to the repo) if picked back up later — ask the user before resuming, this was an explicit "pull back" moment, not just a pause.

22. **Settings restructured into two levels, matching the reference app's card-per-group layout.** Each former `PreferenceCategory` is now a nested `PreferenceScreen` (`settings.xml`) — the top-level screen lists group cards (title + one-line summary of what's inside), tapping one opens a dedicated screen with just that group's items. Deliberately **not** using AndroidX Preference's built-in nested-screen navigation (`onNavigateToScreen` + raw `FragmentManager` transactions) — since `SettingsFragment` lives inside the same `NavHostFragment` every other screen uses, mixing in a second navigation system risked breaking back-press/toolbar-title consistency. Instead: `settingsFragment` gained a self-referencing Nav Component action (`settingsToGroup`, `rootKey`/`groupTitle` nullable args) and `SettingsFragment.onPreferenceTreeClick()` intercepts taps on nested `PreferenceScreen` items to navigate via `findNavController()` like any other screen. **Reusable pattern**: `setPreferencesFromResource(R.xml.settings, rootKey)` already natively supports loading just one nested screen's subtree — this is the standard AndroidX mechanism for multi-key single-file preference screens, worth reaching for again if another screen needs the same "list of groups → detail" shape.

23. **"Clear All Downloads" added to the Music Cache group.** Confirmation-gated (irreversible), reuses the exact same data source and deletion path as Downloads' per-album remove: `offlineMetaDatabase.trackDao().get()` for the full list, `DownloadService.deleteAsync()` to actually delete. `SettingsFragment` now injects `ActiveServerProvider` via Koin — first time it's needed access to the offline DB.

24. **Search visual pass, started: album row modernized.** `AlbumRowDelegate` (shared by Search, `AlbumListFragment`, and `TrackCollectionFragment`'s mixed-folder browsing) was the last row style in the app still showing an always-on rating star with no `Ultrasonic.PrimaryText` title styling and an old `ellipsize="marquee"` pattern — brought in line with every other row this session (star removed, no row-level replacement — same trade-off already made for track rows, not re-asked). Song results in Search were already using the modern shared track row, untouched. Still open if picked back up: whether Search should get its own in-screen search field instead of relying on the collapsible system `SearchView` in the toolbar — a structural change, not just visual, deliberately not pursued alongside the live-filter work (item 19).
25. **Last remaining un-migrated screens brought to the current visual style — the screen-by-screen visual pass is now complete.** Following the user's own 3-step plan ("busca a ver si aún quedan pantallas sin migrar... luego pulimos el player y finalmente hacemos una auditoría de consistencia"), a survey found 4 full screens and 2 minor details still on old styling; all migrated and verified on the Pixel 7 (server switching, Equalizer navigation from Settings, Add Server form — no crashes, no logcat errors):
    - **About** (`help.xml`) and **Lyrics** (`lyrics.xml`): typography brought in line (`Ultrasonic.PrimaryText`/`SecondaryText` + Material3 text appearances), buttons converted to `MaterialButton`. Lyrics also reordered so the song title (not the artist) is the visually prominent first line, matching how the "no lyrics found" fallback message is now displayed.
    - **Configured servers** (`ServerSelectorFragment` + `ServerRowAdapter`): rebuilt from `ListView`/`BaseAdapter` (the last screen in the app still using that pre-RecyclerView pattern) to `RecyclerView`/`RecyclerView.Adapter`. Each server is now a `MaterialCardView` matching Playlists/Downloads, active server shown via a `colorPrimary` stroke instead of a swapped background, add-server moved to a FAB, and the "⋮" menu button fixed from the recurring blurry-`ImageButton` bug to a proper `MaterialButton` with `app:iconSize`.
    - **Equalizer** (`equalizer.xml`/`equalizer_bar.xml`): hardcoded `#c0c0c0` text replaced with theme colors, preset button converted to outlined `MaterialButton`. Pure layout change — `EqualizerFragment.kt` untouched, same view ids.
    - **Add/Edit server** (`server_edit.xml`, minor): found and fixed a real style misuse — four row-label `TextView`s (`edit_server_color_text`, `edit_self_signed_title`, `edit_plaintext_title`, `edit_jukebox_title`) were using `style="@style/Widget.AppCompat.CompoundButton.Switch"`, a style meant for text *inside* a Switch widget, not a plain label sitting next to a separate `SwitchMaterial`. Replaced with the standard `Ultrasonic.PrimaryText` row-label pattern used everywhere else. **Worth grepping for `Widget.AppCompat.CompoundButton.Switch` elsewhere if more legacy screens turn up** — it's an easy copy-paste mistake that still visually "works" (renders readable text) so it doesn't get caught by anything except noticing the style name doesn't match the widget.
    - **Folder-selector list header** (`list_header_folder.xml`, minor): the row shown as item 0 of Artists/Albums/Songs/Genres when a server is in folder-browse mode (not ID3) — typography/icon-tint updated to match. **Not visually verified on-device** — doing so needs a server configured for folder-based browsing, which wasn't available this session; view ids (`select_folder_header`/`select_folder_title`/`select_folder_name`) are unchanged so `FolderSelectorBinder.kt` should be unaffected, but treat this one specific screen as still needing a real look if a folder-mode server becomes available.
26. **Synced (karaoke-style) lyrics, plus an unrelated Player SeekBar bug found and fixed along the way.** The user asked to "pulir las letras" and whether sync + big text was possible — the app had only ever implemented the old artist/title-based `getLyrics.view` (always plain text); the OpenSubsonic extension `getLyricsBySongId` (per-song-id, returns timestamped lines when the server has them) was never wired up. Confirmed live against the user's real Navidrome server that the extension is supported there (200 OK). New API layer (`StructuredLyrics.kt`/`GetLyricsBySongIdResponse.kt`/`SubsonicAPIDefinition.getLyricsBySongId`, deliberately *not* added to `ApiVersionCheckWrapper` since it's an OpenSubsonic extension, not part of the core versioned protocol), domain `Lyrics` gained `synced`/`lines`, and `LyricsFragment` tries the new endpoint first, falling back automatically to the old one (converted into un-timed lines) when the server doesn't support it or a song has no synced data. UI moved from a single `TextView` in a scroll view to a `RecyclerView` of large centered per-line rows (`LyricsLineAdapter`); when real timestamps exist, a 500ms→300ms-style position-polling loop (same pattern as `PlayerFragment`'s existing seek-bar updater) highlights the current line and auto-centers it via smooth-scroll — when they don't, every line just renders large and readable with no movement. A sync-state indicator makes the sync state explicit, since relying on "does it visibly move" alone was too subtle — the user asked directly how they'd know. Went through two rounds of user feedback: (1) a bold all-caps text badge (`Ultrasonic.AllCapsLabel`) below the artist line, shown only when synced — called "grotesco," user asked for a small check/X icon instead, referencing a Spotify screenshot; (2) replaced with a 16dp icon (`ic_lyrics_synced.xml`/`ic_lyrics_unsynced.xml`, standard Material check/close paths), **always shown** once lyrics load (not just when synced — showing "not synced" explicitly closes the original ambiguity better than hiding the badge did) — but the user then asked to relocate it again, inline to the right of the song title instead of its own row, so `lyrics_title` now shares a horizontal row with the icon (`layout_weight="1"` + `ellipsize`/`singleLine` so long titles don't collide with it); the accompanying text label was dropped once the icon moved next to the title (redundant there), surfaced as `contentDescription` instead for accessibility. A direct-access lyrics icon was added to the Player's secondary controls row (reusing the toolbar's existing `ic_library` icon for consistency); that row's 3 buttons (save-playlist/lyrics/queue) are now a centered group with uniform `18dp` horizontal margins per button instead of the old 2-button opposite-ends-plus-spacer layout. Verified end-to-end on the physical device against the real server, in two rounds: a song with no synced data (X icon, big static text, correct), then a song that actually had server-side LRC timing — the check icon appeared and the current line visibly highlighted/auto-scrolled during playback, confirming the karaoke behavior works live, not just the plain-text fallback. **Unrelated bug found via user report while testing this**: `PlayerFragment.updateSeekBar()` runs on the same 500ms polling loop that also drives the progress bar, and was unconditionally overwriting `progressBar.progress` regardless of whether the user had a finger on it — while dragging backward, the thumb kept snapping back forward before the user could release, making backward seeking feel broken. Fixed with an `isSeekBarDragging` flag set in `onStartTrackingTouch`/cleared in `onStopTrackingTouch`, skipped by `updateSeekBar()` while active. User-confirmed fixed on-device.

27. **Final Player polish and cross-screen consistency audit completed.** The full Player now uses a tighter vertical rhythm, larger usable artwork, a stronger `TitleLarge` song title, compact transport/secondary controls, and a real `Now Playing` toolbar title instead of the debug app name. The duplicate always-visible toolbar queue action was removed (the explicit secondary-row queue button is now the single visible entry point), and Save Playlist moved to overflow because the `+` button already exposes it. The deliberate audit compared Home/Artist/Album/Player/Playlists/Downloads patterns, preserved valid action-vs-status differences, and normalized the only objective card-radius drift found (5dp→the established 4dp baseline in Playlists, Downloads, and server rows). Previously pending real-device checks are also complete: every Settings group/back path, a switch round trip, Equalizer, Downloads with real local content/detail navigation, and queue/Favorite/Rate menu visibility all passed on the Pixel 7 without crashes. Destructive download removal and server-side rating mutation were deliberately not executed.
28. **Music-only surface audit completed: Bookmarks now hidden like Podcasts/Video/Chat/Shares, and video results no longer leak through Search.** Bookmarks was removed from the drawer and Player overflow, its global Safe Args action/menu handling was removed, and the already-hidden legacy destinations are no longer treated as top-level app destinations. Their API/models/fragments remain internally for safety and upstream compatibility. One residual path was found during the audit: `search3` can return `Track.isVideo` entries mixed into song results; `SearchFragment` now filters them from list expansion and autoplay so tapping Search can never launch the legacy video player. Server capability reporting in Test Connection remains diagnostic-only, not a product entry point.
29. **Drawer migration completed: bottom navigation plus a dedicated library/server hub.** The main shell now exposes Home, Library, Search, and Downloads in a persistent `BottomNavigationView`, with the mini-player immediately above it and the full Player still entered through the mini-player. The bottom bar is hidden on Player and auxiliary/settings/server screens, and Downloads becomes unavailable offline. A top-right “Your library” action replaces the drawer header and preserves the previously loose server flows: it identifies the active server and links to Switch library, Add library, Settings, and About. Search owns its expanded toolbar field only while its tab is selected. The obsolete drawer fields, listeners, header coloring, and back callback were removed from `NavigationActivity`; the legacy drawer resource can remain inert for upstream compatibility. Build, API tests, app unit tests, and debug APK assembly pass. Global lint still has 1,683 pre-existing repository-wide errors and is not currently a clean gate.
30. **Home and Library now follow the Taki visual direction instead of retaining a global heavy header.** Home hides the shared action bar and starts directly with its greeting, quick-access chips, and content; a three-dot button in the greeting row anchors the shared library popup. Library also hides the shared bar and owns an in-content `Library` heading plus a labeled `Your library` action, so library/server management remains discoverable without an unexplained device icon. Search and contextual/detail destinations restore the action bar normally. `showLibraryHub()` now accepts an optional anchor view so Home, Library, and toolbar-based destinations share one behavior. Verified on the Pixel 7, including popup actions and the hidden→restored toolbar transition into Search; unit tests and debug assembly pass.
31. **About now presents Taki as the product while preserving required upstream attribution.** Its title is `Taki (version)`, followed by the brand tagline and a concise personal/shared-library description. Ultrasonic appears only in the GPLv3/upstream credit paragraph. The existing Ultrasonic URLs remain wired for now but their buttons are explicitly labeled as the original project website and original issue tracker, avoiding the implication that Taki already has official public endpoints. Verified visually on the Pixel 7; Kotlin compilation and debug assembly pass.
32. **Taki starts its own public version line at `0.1.0-beta`.** Only `versionName` was reset; Android `versionCode` remains 131 so installed builds can continue upgrading monotonically instead of being rejected as a downgrade. About now uses the user-approved online/offline + Navidrome/Subsonic/OpenSubsonic copy. Its future web action is named `Visit website` but hidden until Taki has a genuinely public project page, avoiding private-repository 404s or misleading upstream links.
33. **The local repository is connected to the new private GitHub home.** `origin` is now `https://github.com/churipakinti/taki-android.git`; the original GitLab repository was renamed to `upstream`, and its push URL is deliberately `DISABLED` to prevent accidental publication there. Authentication was completed through Git Credential Manager and `git ls-remote origin` succeeds. GitHub is currently empty, so no branch tracks `origin` yet; the first intentional commit/push should publish `develop` (or a newly agreed default branch) and establish tracking.
34. **Search, Downloads, and top-level Settings no longer retain the heavy shared toolbar.** Search owns an in-content rounded `SearchView` and moved live debounce/submit/recent-query behavior from `NavigationActivity` into `SearchFragment`; voice/search intents still route through the existing Safe Args path. Downloads has a dedicated content-title layout. Top-level Settings has an integrated heading, while nested groups intentionally restore the shared toolbar and Up navigation. The obsolete search/library toolbar menu providers and `library_hub_action.xml` were removed. Verified on Pixel 7 with a real `Queen` query and a Settings group round trip; Kotlin compilation and debug APK assembly pass.
35. **The full Player is now immersive and no longer uses the shared `Now Playing` toolbar.** Back and overflow controls live in the Player content in portrait and landscape; the overflow reuses the complete existing player menu and the bottom navigation remains hidden. Device testing also exposed a pre-existing Home lifecycle toast (`Fragment HomeFragment…`) when navigation beat an in-flight load: `HomeFragment.load()` now uses `viewLifecycleOwner.lifecycleScope` instead of the longer-lived ViewModel scope, so UI work is cancelled with the view. Verified on Pixel 7 and with debug assembly.
36. **Lyrics now follows the same immersive navigation pattern as the Player.** The shared toolbar is hidden on the lyrics destination and Back is integrated into its existing song/artist/sync-status header. Synced and plain lyrics loading, highlighting, and automatic scrolling remain unchanged.
37. **Fixed a toolbar-migration crash in `TrackCollectionFragment`.** Its menu provider still dereferenced the removed global `action_search` during `onPrepareMenu`, producing an NPE when returning to album/track collections. That obsolete operation was removed and Play all configuration now uses a nullable menu-item reference.
38. **Fixed Player startup in landscape.** The landscape `current_playing.xml` omitted `player_secondary_controls`, while `PlayerFragment.findViews()` requires Queue, Lyrics, and Save Playlist in every layout variant. The shared secondary-controls include is now present in landscape as it already was in portrait.
39. **The Taki brand system is now implemented globally instead of relying on Android dynamic color.** `UltrasonicTheme.Dark` owns the exact manual tokens (lime/black/deep black/surface/ivory/gray/track and muted progress), including system bars, Material surfaces, preferences, widget, and checked-state bottom navigation. The adaptive/monochrome launcher icon uses the official Taki note, and a non-translatable Taki resource protects all visible application/service/widget/navigation labels from stale localized `common.appname` overrides. Internal package/class identifiers deliberately remain Ultrasonic for compatibility. Landscape Player was rebuilt as a calm two-column counterpart to portrait with clean artwork and controls, removing the enlarged background and legacy five-star strip.
40. **The Taki accent was softened and made semantic.** Central tokens are now main `#B7D63C`, pressed/secondary-active `#91AD30`, progress `#7C9229`, selected container `#293217`, and on-accent `#11130D`; dark backgrounds and neutrals are unchanged. Full accent is restricted to primary action/selected navigation, secondary Player actions use the 80% token, progress is deliberately quieter, and tonal chips use the dark selected container. The launcher consumes the central token and the old lime values have no remaining resource references. WCAG contrast checks pass (main/on-accent 11.31:1; secondary/background 7.77:1; progress/background 5.67:1; gray/background 8.72:1).
41. **Playlists now behaves correctly as a Home child destination.** Its obsolete toolbar title call was removed and the Activity treats it as content-header/no-global-toolbar. Bottom-navigation reselection now explicitly pops to the selected tab root (Home, Library, Search, or Downloads), fixing both the original Playlists→Home case and detail/list→Library equivalents where the tab remained visually selected and tapping it previously did nothing. Verified in the emulator: zero toolbar nodes on Playlists and Home greeting restored after tapping Home.
42. **Library is now a collection index rather than a duplicate tabbed browser.** Home quick access is limited to Playlists/Albums/Artists/Songs (Genres removed). Library groups Liked Songs and Playlists under `Your music`, then Albums/Artists/Songs/Genres under `Collection`; Liked Songs reuses the existing `getStarred` track route and all other rows reuse established destinations. The obsolete internal ViewPager, tabs, filter bar, adapter, and subscriptions were removed from `MainFragment`; `FilterableFragment` remains because destination list fragments still implement it. `Your library` remains a separate server/settings action.
43. **Playback session restoration is now reliable across abrupt process death.** Ultrasonic already serialized the queue, current index/position, shuffle and repeat, and restored them without autoplay during a normal app launch. Taki completes that path with a five-second position checkpoint while playing and immediate persistence events for shuffle/repeat. The first-use callback also runs when no saved state exists. Verified on the physical Pixel 7 with a two-song queue: force-stopping at 43.858s restored index 1/2 at exactly 43.858s, paused; shuffle=true/repeat-all also survived a separate force-stop, then the test state was returned to shuffle off/repeat off.
44. **The visible queue now has its own Taki presentation instead of the generic legacy track row.** It remains inside Now Playing's existing flipper (not a new bottom sheet), but adds a Queue/count heading, 52dp artwork, compact title/artist hierarchy, right-side 48dp drag targets, and a green equalizer/title treatment only for the current track. Tap-to-play, drag reorder, and swipe-to-remove are unchanged. Compiled, installed, screenshot-checked and drag-tested on the Pixel 7; the two-item test queue was returned to its original order afterward.

## Pending / next steps

- **Current priority (2026-08-09): work through `docs/AUDITORIA_FUNCIONAMIENTO_INTERNO.md`.** The user considers the visual/UX pass done — moderno y funcional — and the next phase is a deep internal-correctness/performance audit, not more visual work. That document is the actual task list: known bug patterns from this project's history (fixed-instance `CoroutineScope` delegates outliving a Fragment's view, unguarded pagination, main-thread Media3/DB access, unclosed cursors), two concrete unconfirmed suspects already flagged (`PlayerFragment.ioScope` never cancelled/reassigned — `savePlaylistInBackground()` may toast after the view is destroyed instead of using the existing `Fragment.launchWithToast()` helper; `TrackViewHolder`'s per-instance `CoroutineScope` on a recycled `ViewHolder`), a two-layer methodology (static grep sweep, then StrictMode-driven dynamic verification on the Pixel 7 — StrictMode is already enabled in debug via `UApp.kt`), and the critical flows to trace end-to-end. Follow that document rather than re-deriving an audit approach from scratch. Log confirmed findings in `CHANGES.md` with the same before/after detail as past bug fixes.
- **The agreed 3-step visual pass is complete (2026-08-09): (1) migrate remaining old screens, (2) polish the Player, (3) run the design-consistency audit.** See items 25 and 27 above. The next work should be a separately scoped functional feature rather than more undirected visual churn.
- **Search visual pass completed (2026-08-09):** it now has a content heading, a neutral outlined search field, distinct initial/no-results copy, normalized section spacing, and a 48dp Material “Show more” action. Settings and Downloads are also done; Shares no longer needs a pass because it is hidden from the UI.
- **Playback queue persistence completed (2026-08-09):** queue, current track, position, shuffle and repeat restore across a restart without automatic playback. See item 43 and `CHANGES.md`.
- Most other recent visual passes (through item 15) have been verified on a Pixel 7 across several on-device screenshot rounds — see `CHANGES.md` for the specific fixes that came out of each round (back-press in selection mode, FAB shape, icon blur, disc-header spacing, etc.).
- `Settings.showNowPlayingDetails` and its preference were already deleted after the related Player metadata left the screen; older notes describing it as pending are obsolete.
- The work is checkpointed in local commits on `develop`; inspect `git log` before starting a
  new visual pass. Never push to the configured upstream without a separate explicit request.

## Key files

- `ultrasonic/src/main/kotlin/org/moire/ultrasonic/fragment/HomeFragment.kt` + `model/HomeViewModel.kt` — Home screen and its data/Mix logic.
- `ultrasonic/src/main/kotlin/org/moire/ultrasonic/adapters/Home*.kt` — MultiType delegates for Home's cards.
- `ultrasonic/src/main/res/layout/home_fragment.xml`, `home_*_item.xml` — Home's layouts.
- `ultrasonic/src/main/res/layout/now_playing.xml` + `fragment/NowPlayingFragment.kt` — mini player bar.
- `ultrasonic/src/main/res/layout/current_playing.xml`, `player_media_info.xml`, `player_slider.xml`, `media_buttons.xml` + `fragment/PlayerFragment.kt` — full player screen.
- `ultrasonic/src/main/kotlin/org/moire/ultrasonic/fragment/ArtistListFragment.kt`, `AlbumListFragment.kt`, and `TrackCollectionFragment.kt` — redesigned library entry points and the current routes that future artist/album detail work must split or refine.
- `ultrasonic/src/main/kotlin/org/moire/ultrasonic/fragment/ArtistDetailFragment.kt` + `model/ArtistDetailModel.kt` + `artist_detail.xml` — dedicated artist hero/actions/songs/albums screen.
- `ultrasonic/src/main/kotlin/org/moire/ultrasonic/adapters/AlbumDetailHeaderBinder.kt` + `album_detail_header_item.xml` + `list_item_track_album.xml` — Album Detail's hero/track-row layer, scoped inside `TrackCollectionFragment` via `navArgs.isAlbum`; `TrackViewBinder`/`TrackViewHolder` gained optional `layout`/`showArtist`/`showRating`/`trackNumberText` params to support it without touching the shared row logic used by other screens.
- `core/subsonic-api/src/main/kotlin/org/moire/ultrasonic/api/subsonic/SubsonicAPIDefinition.kt` + `models/ArtistInfo.kt` — optional artist biography/similar-artists and real top-song endpoints.
- `ultrasonic/src/main/kotlin/org/moire/ultrasonic/fragment/legacy/PlaylistsFragment.kt` + `playlist_cover_collage.xml`, `list_item_playlist.xml`, and `grid_item_playlist.xml` — functional playlist list/grid and download state.
- `ultrasonic/src/main/kotlin/org/moire/ultrasonic/util/Settings.kt` / `SettingsDelegate.kt` — SharedPreferences-backed state pattern to reuse for any new persisted setting.
- `ultrasonic/src/main/kotlin/org/moire/ultrasonic/fragment/DownloadsFragment.kt` (album cards, `MultiListFragment<Album>`) + `DownloadedAlbumFragment.kt` (local-only per-album track view, `TrackCollectionFragment` subclass) + `adapters/DownloadedAlbumRowBinder.kt` + `list_item_downloaded_album.xml` — the download manager.
- `CHANGES.md` — the real changelog. Read it before assuming something hasn't been tried.
